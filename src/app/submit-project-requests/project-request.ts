export class ProjectRequest {
	
	constructor(
		title: string,
		description: string,
		technologyStack: string[],
		expectedTimeLineStart: Date,
		expectedTimeLineEnd: Date,
		budgetStart: number,
		budgetEnd: number
	){}
}
